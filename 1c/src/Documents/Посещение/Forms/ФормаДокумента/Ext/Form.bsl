#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Объект.Ссылка.Пустая() Тогда
		Объект.Дата           = ТекущаяДата();
		Объект.ВремяПосещения = ТекущаяДата();
	КонецЕсли;
КонецПроцедуры

&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
КонецПроцедуры

&НаКлиенте
Процедура КлиентПриИзменении(Элемент)
	ЗаполнитьАбонементНаСервере();
КонецПроцедуры

&НаСервере
Процедура ЗаполнитьАбонементНаСервере()
	Если Объект.Клиент.Пустая() Тогда
		Возврат;
	КонецЕсли;
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ ПЕРВЫЕ 1
	| АбонементыКлиентов.Ссылка
	|ИЗ
	| Справочник.АбонементыКлиентов КАК АбонементыКлиентов
	|ГДЕ
	| АбонементыКлиентов.Клиент = &Клиент
	| И АбонементыКлиентов.Активен = ИСТИНА
	| И АбонементыКлиентов.ОстатокПосещений > 0
	|УПОРЯДОЧИТЬ ПО
	| АбонементыКлиентов.ДатаОкончания УБЫВ";
	Запрос.УстановитьПараметр("Клиент", Объект.Клиент);
	Результат = Запрос.Выполнить();
	Если НЕ Результат.Пустой() Тогда
		Объект.АбонементКлиента = Результат.Выгрузить()[0].Ссылка;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗаписи(ПараметрыЗаписи)
	Оповестить("ОбновлениеДокумента_Посещение");
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытий

Процедура ОбработкаПроведения(Отказ, РежимПроведения)

	Если Не ЗначениеЗаполнено(АбонементКлиента) Тогда
		Отказ = Истина;
		Сообщить("Не указан абонемент клиента.");
		Возврат;
	КонецЕсли;

	Если КоличествоПосещений <= 0 Тогда
		Отказ = Истина;
		Сообщить("Количество посещений должно быть больше нуля.");
		Возврат;
	КонецЕсли;

	Движения.ОстаткиАбонементов.Записывать = Истина;

	ДвижениеОстатка = Движения.ОстаткиАбонементов.Добавить();
	ДвижениеОстатка.ВидДвижения = ВидДвиженияНакопления.Приход;
	ДвижениеОстатка.Период = Дата;
	ДвижениеОстатка.АбонементКлиента = АбонементКлиента;
	ДвижениеОстатка.КоличествоПосещений = КоличествоПосещений;

КонецПроцедуры

#КонецОбласти